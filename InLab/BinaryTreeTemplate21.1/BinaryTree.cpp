template<class T>
void BinaryTree<T>::insert(TreeNode *&nodePtr, TreeNode *&newNode){
  if(nodePtr == nullPtr){
    nodePtr = newNode;
  }
  else if(newNode->)
}

template<class T>
void BinaryTree<T>::insertNode(T num){
  TreeNode *newNode = nullptr;
}

template <class T>
void BinaryTree<T>::destroySubTree(TreeNode *nodePtr){
  if(nodePtr->left)
}

//remove function, just calls the deletenode function

//delete node function

//search function

//make deletion function, need to fix all of the pointers when deleting
//somethign in the middle of the list

//display function, display the tree in the order of top to down

//display pre order

//display post order